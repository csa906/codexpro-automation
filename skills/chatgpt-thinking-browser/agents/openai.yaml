interface:
  display_name: "ChatGPT Thinking Browser"
  short_description: "Run scoped Oracle missions at the appropriate Power"
  default_prompt: "Use $chatgpt-thinking-browser to run this project mission through Oracle at the appropriate Power."
policy:
  allow_implicit_invocation: false
