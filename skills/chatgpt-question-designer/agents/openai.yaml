interface:
  display_name: "ChatGPT Question Designer"
  short_description: "Design Oracle+DevSpace missions, including read-only Pro routes."
  default_prompt: "Design a purpose-specific GPT mission using Oracle+DevSpace; qualified Pro is read-only and `pro-attachment` is only for DevSpace-unreadable immutable evidence. Never route new work through CodexPro."
